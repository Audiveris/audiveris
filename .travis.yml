language: java
addons:
  apt:
    packages:
    - tesseract-ocr
    - tesseract-ocr-dev
    - tesseract-ocr-eng

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
